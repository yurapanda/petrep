<%= simple_form_for [@patient, @weight] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :kilograms %>
    <%= f.input :current %>
   
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
