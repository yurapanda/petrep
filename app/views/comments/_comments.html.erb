<h3> Comments: </h3>
<div class="well">
<ul>
  <% commentable.comments.each do |comment| %>

    	<li> <%= comment.body %> </li>
      <li> <%= time_ago_in_words(comment.created_at) + ' ago' %> by
       <%= comment.user.email %> </li>
    
  <% end %>
  </ul>
</div>
